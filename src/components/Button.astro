---
const { href, target, flipped } = Astro.props;
const flippedClass = flipped ? "" : "bg-red-800";
---

<a
  class=`text-slate-200 text-xl px-5 py-2 my-2 border-solid border-red-500 border-2 rounded-md inline-block no-underline ${flippedClass}`
  href={href}
  target={target}
  ><slot />
</a>
